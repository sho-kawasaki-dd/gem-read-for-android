package io.github.ikinocore.gemread.android.ui.base

/**
 * UI State. Represents the current state of the UI.
 */
interface UiState

/**
 * UI Event. Actions from UI to ViewModel.
 */
interface UiEvent

/**
 * UI Effect. One-off events from ViewModel to UI (e.g. navigation, snackbar).
 */
interface UiEffect
